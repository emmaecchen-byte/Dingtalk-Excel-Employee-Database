# Backend origin (no /api suffix). Leave empty to use the Vite dev-server proxy (/api → localhost:8000).
# Set this when opening the frontend from a LAN IP, e.g. http://172.16.20.7:5173
VITE_API_BASE_URL=http://172.16.20.7:8000
